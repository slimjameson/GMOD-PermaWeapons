This is a simple addon for Garrys Mod anyone can use!

!permweapons to open it 

It uses SteamID not SteamID64 for setting the perm swep!

It saves it locally in the GMod Data folder

There is also a config file to set what usergroups can use it
